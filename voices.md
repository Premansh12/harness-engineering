# Leader voices — harvested 2026-08-02

Real, dated posts read directly from public X profiles via the browser (no auth
needed for public profiles). Each is quoted verbatim from the rendered profile
and linked to the canonical profile URL. Dates are as shown on the profile.

Provenance rule: only quotes actually observed on a loaded public profile are
included. No X search was possible (search requires login); handles were
reached directly. Two attempts (Birgitta Böckeler, HumanLayer) failed or 404'd
and are NOT represented here — do not fabricate their quotes.

## Andrej Karpathy (@karpathy) — https://x.com/karpathy
- Jul 21: "One pattern I find useful for working with LLMs is a nice long ramble session. Sometimes the LLM needs more bits to understand what you're trying to achieve, but you're too lazy to type them..."
  Context: general LLM usage pattern, not harness-specific. Usable as a voice on
  working-with-models style, NOT as a harness claim.
- Jun 23: "This is a new paradigm for interacting with Claude that is significantly more 'inline' with all the other human activity org-wide. Once you do all of the under the hood engineering work to make this 'just work' (e.g. across tools, integrations, compute environments, memory..." 
  Context: commenting on Claude Tag / inline agent integration — TOUCHES harness
  themes (tools, integrations, compute, memory) but is a product comment.

## Addy Osmani (@addyosmani) — https://x.com/addyosmani
- Jul 30: "Software quality now depends on the constraints you set around your agents. When humans manually wrote most of the code we could look at the code itself for signs of quality. Is it clean? Is it thoughtful? Is it fast? Can another engineer understand it? Does it have tests?"
  Context: DIRECTLY harness-relevant — permissions/constraints as the new quality
  surface. Strong fit for Module 07 (safety/permissions) or 00 (definition).
- Jul 21: "Loops, harnesses, factories and why engineers need to own the outer loop."
  Context: DIRECTLY on-topic — names harnesses and the outer loop. Fit for Module
  01 (the loop) or 09 (the discipline of deleting/owning the loop).
- Jul 21 (article): "A software factory is harnessed loops at scale. You can run
  the loop with humans in it (light factory): trading judgment and concentration
  against speed and breakage. Or you can ignore the humans..."
  Context: harnessed loops at scale; fit for Module 06 (long horizons) or 09.

## Notes for the build
- Karpathy's visible posts are general-LLM, not harness-specific; if used, label
  them as "on working with models" not "on harnesses."
- Osmani's Jul 30 + Jul 21 posts are the strongest harness-relevant primary
  voices and should anchor the "leader voices" callouts.
- Do NOT attribute harness claims to Karpathy's ramble/inline posts.
- If the user later authenticates xurl (or provides Böckeler/HumanLayer handles),
  re-harvest and expand this file.
